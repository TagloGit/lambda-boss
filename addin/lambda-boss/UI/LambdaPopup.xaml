<Window x:Class="LambdaBoss.UI.LambdaPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lambda Boss"
        Width="400" Height="300"
        WindowStyle="ToolWindow"
        ShowInTaskbar="True"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        Background="#1e1e1e">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search box -->
        <TextBox x:Name="SearchBox"
                 Grid.Row="0"
                 Margin="0,0,0,8"
                 Padding="6,4"
                 FontSize="14"
                 Background="#2d2d2d"
                 Foreground="#cccccc"
                 CaretBrush="#cccccc"
                 BorderBrush="#3e3e3e"
                 BorderThickness="1"
                 TextChanged="SearchBox_TextChanged" />

        <!-- Lambda list -->
        <ListBox x:Name="LambdaList"
                 Grid.Row="1"
                 Background="#252526"
                 Foreground="#cccccc"
                 BorderBrush="#3e3e3e"
                 BorderThickness="1"
                 FontFamily="Consolas"
                 FontSize="13"
                 SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <!-- ReSharper disable Xaml.BindingWithContextNotResolved -->
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#dcdcaa" />
                        <TextBlock Text="  " />
                        <TextBlock Text="{Binding Formula}" Foreground="#808080" />
                        <!-- ReSharper restore Xaml.BindingWithContextNotResolved -->
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status bar -->
        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Margin="0,4,0,0"
                   Foreground="#808080"
                   FontSize="11"
                   Text="Enter to load · Escape to close" />
    </Grid>
</Window>